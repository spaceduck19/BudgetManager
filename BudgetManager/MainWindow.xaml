<Window x:Class="BudgetManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BudgetManager"
        xmlns:utils="clr-namespace:BudgetManager.Utils"
        mc:Ignorable="d"
        Title="Pénzügyi Menedzser" Height="450" Width="600">
    <Window.Resources>
        <utils:AmountColorConverter x:Key="ColorConv" />
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding CurrentBalance, StringFormat='Egyenleg: {0} Ft'}" 
                   FontSize="24" FontWeight="Bold" TextAlignment="Center" Margin="10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" 
            ButtonBase.Click="CategoryFilterBar_Click" Margin="0,0,0,10">

            <TextBlock Text="Szűrés kategóriára: " VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>

            <ItemsControl ItemsSource="{Binding Categories}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding}" Margin="5" Padding="5"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <ListBox Grid.Row="2" ItemsSource="{Binding DisplayedTransactions}" SelectedItem="{Binding SelectedTransaction}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="{Binding Title}" Width="150" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Category}" Width="100"/>
                        <TextBlock Text="{Binding Amount, StringFormat='{}{0} Ft'}" 
                                   Foreground="{Binding Type, Converter={StaticResource ColorConv}}" FontWeight="Bold"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Új Hozzáadása" Command="{Binding CreateNewCommand}" Margin="5" Padding="10" Background="LightGreen"/>
            <Button Content="Szerkesztés" Command="{Binding EditSelectedCommand}" Margin="5" Padding="10"/>
            <Button Content="Törlés" Command="{Binding DeleteSelectedCommand}" Margin="5" Padding="10" Background="LightCoral"/>
        </StackPanel>
    </Grid>
</Window>